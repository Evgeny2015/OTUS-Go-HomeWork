[logger]
level = "INFO"

[storage2]
type = "memory"
# dsn = "host=localhost port=5432 user=postgres password=postgres dbname=calendar sslmode=disable"

[storage]
type = "sql"
dsn = "host=localhost port=5432 user=postgres password=postgres dbname=calendar sslmode=disable"

[http]
host = "localhost"
port = "8080"
